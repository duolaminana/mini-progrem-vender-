/* pages/signIn/component/getUserPhone/getUserPhone.wxss */
@import '/static/css/$class.wxss';
.cus-layer{
	z-index: calc(var(--zindex_zzc) + 10);
}
.com-initButton{
    position: fixed;
    left: 0;
    top: 0;
	width: 100%;
	height: 100%;
	z-index: calc(var(--zindex_zzc) + 10);
	display: flex;
	align-items: center;
	justify-content: center;
}
.content{
	width: calc(289rpx * var(--bs));
	background-color: var(--wc);
	box-sizing: border-box;
	padding: 47rpx;
	padding-top: 60rpx;
	padding-bottom: 34rpx;
	border-radius:10rpx;
	position: relative;
}
button{
	background-color: transparent;
	font-size: 30rpx;
	font-weight: var(--w5);
	width: 100%;
	height: 88rpx;
	display: flex;
	align-items: center;
	justify-content: center;
}
button::after{
	display: none;
}
.shortcut{
	background-color: #3FCF77;
	color: var(--wc);
	margin-bottom: 10rpx;
	padding: 0;
}
.shortcut image{
	width: 60rpx;
	height: 50rpx;
	padding-right: 26rpx;
}
.nav-warp{
	padding: 0;
}
.nav-warp navigator{
	width: 100%;
	height: 100%;
	line-height: 88rpx;
}
.nav-warp navigator{
	width: 100%;
	height: 100%;
	line-height: 88rpx;
}
.gosignIn{
	color: var(--zc3);
}
.close{
	width: 32rpx;
	height: 32rpx;
	position: absolute;
	top: 0;
	right: 0;
	padding: 15rpx;
}
.errmsgajax{
	position: fixed;
	top: 0;
	left: 0;
	width: 100%;
	height: 100vh;
	background-color: var(--wc);
	z-index: calc(var(--zindex_hd) + 110);
}